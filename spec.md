# BoxPet — ESP32 电子宠物（类拓麻歌子）规格说明书

| 项目 | 内容 |
|---|---|
| 项目代号 | BoxPet |
| 目标硬件 | 正点原子 ESP32 AI BOX0（ATK-DNESP32S3B0） |
| 版本 | v1.0（草案，待评审） |
| 日期 | 2026-08-20 |

---

## 1. 项目概述

在正点原子 ESP32 AI BOX0 上开发一款单机电子宠物软件，完整复刻初代拓麻歌子（Tamagotchi）的核心玩法：孵化、喂养、清洁、陪玩、管教、医疗、成长进化、生病与死亡的全生命周期养成体验，并利用 BOX0 的彩色 IPS 屏与喇叭提供像素动画与音效。

**非目标（本期不做）**：
- 联网功能（AI 对话、云同步、SNTP 校时为二期可选项）
- 触摸操作（BOX0 无触摸屏，仅用 3 个物理按键，正好对应初代拓麻歌子的 3 键设计）
- 多宠物同时饲养

---

## 2. 目标硬件与开发环境

### 2.1 硬件规格（已核实）

| 组件 | 规格 | 关键引脚 |
|---|---|---|
| 主控 | ESP32-S3R8，240MHz 双核，512KB SRAM，8MB PSRAM（八线），16MB Flash | — |
| 屏幕 | 1.54 寸 IPS，240×240，ST7789 驱动（SPI） | SCLK=GPIO39, MOSI=GPIO40, DC=GPIO38, CS=GPIO41, 背光=GPIO42 |
| 音频 | ES8311 编解码器 + 板载喇叭 | I2S: MCLK=13, WS=10, BCLK=5, DIN=6, DOUT=9；I2C: SDA=11, SCL=12；喇叭使能=GPIO21 |
| 按键 | 3 个物理按键 | 左=GPIO3，中=GPIO4，右=GPIO0 |
| 电池 | 500mAh 锂电池，Type-C 充电 | 电压检测=GPIO1（ADC），充电状态=GPIO48 |
| 电源 | 板载电源管理 | SYS_POW=GPIO2，CHG_CTRL=GPIO47，CODEC_PWR=GPIO14 |

引脚定义来源：xiaozhi-esp32 项目 `main/boards/alientek/atk-dnesp32s3-box0/config.h`（该固件官方支持 BOX0，引脚映射可靠）。

### 2.2 软件环境

| 项 | 选择 | 理由 |
|---|---|---|
| 框架 | ESP-IDF v5.4（C++） | 正点原子官方教程体系，BOX0 原厂固件同框架 |
| GUI | LVGL 9（组件库 `lvgl/lvgl` + `espressif/esp_lvgl_port`） | 成熟的菜单/动画/字体能力，适配 ST7789 |
| LCD 驱动 | 组件库 `espressif/esp_lcd_st7789` + `esp_lcd` | 官方并行支持 |
| 音频 | `espressif/esp_codec_dev` 组件（已核实 API）+ 自研音效合成器 | ES8311 寄存器初始化交给官方组件（`audio_codec_new_i2c_ctrl` → `es8311_codec_new` → `audio_codec_new_i2s_data` → `esp_codec_dev_open/write`），上层只做 16kHz/16bit PCM 合成音 |
| 持久化 | NVS（内置，无需分区改动） | 数据量小（<200 字节） |

### 2.3 屏幕方向与缓冲

- 240×240 正方形屏幕，横竖任意；宠物画面居中。
- LVGL 双缓冲帧放 PSRAM（240×240×2B×2 ≈ 230KB），SPI 时钟 40MHz，刷新足够流畅。

---

## 3. 游戏设计规格（完整拓麻歌子功能）

### 3.1 时间体系

支持两种时间模式（设置菜单可切换）：

| 模式 | 1 宠物日 = | 用途 |
|---|---|---|
| 演示模式（默认） | 1 真实小时 | 完整生命周期约 11 小时跑通，便于体验与演示 |
| 真实模式 | 24 真实小时 | 忠实还原初代节奏 |

宠物时间来源：开机累计运行时间（**关机即暂停**，等价于初代的暂停功能，简单可靠）。宠物的"日历时刻"（几点几分）由宠物纪元秒换算，用于作息调度。

### 3.2 生命周期与进化

| 阶段 | 演示模式时长 | 真实模式时长 | 说明 |
|---|---|---|---|
| 蛋 | 1 分钟 | 5 分钟 | 摇晃动画，孵化音效 |
| 婴儿 | 1 宠物日 | 1 宠物日 | 衰减最快，最需要照顾 |
| 儿童 | 2 宠物日 | 2 宠物日 | — |
| 少年 | 2 宠物日 | 2 宠物日 | — |
| 成年 | 5 宠物日 | 5 宠物日 | 进化分支由照顾评分决定 |
| 老年 | 3 宠物日 | 3 宠物日 | 动作变慢，睡眠变长 |
| 死亡 | — | — | 见 3.6 |

**成年进化分支**（照顾评分 = 饥饿满足率×40% + 快乐满足率×40% + 纪律×20%，在进化时刻计算）：

| 评分 | 形态 | 角色名（自创，避免版权） |
|---|---|---|
| ≥ 80% | 优秀形态 | 「星宝」 |
| 50%–79% | 普通形态 | 「团团」 |
| < 50% | 懒散形态 | 「躺躺」 |

### 3.3 属性系统

| 属性 | 范围 | 变化来源 |
|---|---|---|
| 饥饿 hunger | 0–4（心形） | 喂饭 +1；随时间衰减 |
| 快乐 happiness | 0–4（心形） | 零食 +1、游戏获胜 +1；随时间衰减 |
| 纪律 discipline | 0–100% | 撒娇呼叫时管教 +10% |
| 体重 weight | 克 | 喂饭 +1g、零食 +2g、游戏胜利 −2g |
| 年龄 age | 宠物日 | 随时间 |
| 疾病 sick | 布尔 | 见 3.6 |
| 便便 poop | 0–4 个（屏幕可见） | 定时产生，清洁清除 |
| 照顾失误 careMistakes | 计数 | 影响进化评分 |

### 3.4 时间衰减规则（演示模式数值；真实模式等比 ×24）

| 事件 | 周期（真实时间） | 效果 |
|---|---|---|
| 饥饿衰减 | 每 12 分钟 −1 心 | 饥饿=0 后进入"饥饿状态"，持续 30 分钟未喂 → 生病概率大增 |
| 快乐衰减 | 每 15 分钟 −1 心 | — |
| 便便产生 | 每 18 分钟 1 个（婴儿/老年更快） | 便便 ≥4 → 立即生病 |
| 作息 | 宠物日 22:00 入睡，07:00 醒 | 睡觉时需关灯（见 3.5 灯光） |
| 撒娇呼叫 | 每宠物日随机 1–3 次 | 无故 beep，需管教 |
| 属性停止衰减 | 睡眠期间 | 饥饿/快乐不衰减（还原初代） |

### 3.5 八大图标功能（核心操作）

屏幕上/下两排共 8 个图标（对应初代）：

| # | 图标 | 功能 | 规则 |
|---|---|---|---|
| 1 | 🍚 喂食 | 子菜单：饭（meal）/ 零食（snack） | 饭：hunger+1、体重+1g；零食：happiness+1、体重+2g。hunger=4 或生病时拒绝进食并摇头 |
| 2 | 💡 灯光 | 开/关灯 | 睡觉时灯亮 → 快乐每 10 分钟 −1；非睡眠时关灯仅调暗屏幕 |
| 3 | 🎮 游戏 | 左右猜方向小游戏（5 回合） | 胜（≥3 中）：happiness+1、体重−2g；负：体重不变。生病/睡觉不能玩 |
| 4 | 💊 医疗 | 喂药 | 生病时每次 1 剂，需随机 1–4 剂治愈；每剂播放"苦"表情。非生病时吃药 → 苦脸拒绝 |
| 5 | 🚽 清洁 | 冲走全部便便 | 播放冲水动画+音效；便便=0 时无效 |
| 6 | 📊 状态 | 分页查看（左右翻页，共 3 页） | 页1：年龄/体重；页2：饥饿/快乐心形；页3：纪律%/照顾失误/时间模式 |
| 7 | 😤 管教 | 责骂 | 仅在"撒娇呼叫"激活时有效：discipline+10%，宠物委屈动画；平时使用 → 快乐 −1（还原初代惩罚误管教） |
| 8 | 🔔 注意（指示器） | 不可选中，自动点亮 | 宠物有需求时闪烁：饥饿≤1、快乐≤1、生病、该睡觉了、便便≥3 |

### 3.6 疾病与死亡

**生病触发**：
- 便便 ≥4 且未清理；
- 饥饿=0 持续 30 分钟；
- 随机事件：基础 2%/宠物日，每 1 次照顾失误 +3%。

**生病表现**：病恹恹动画（骷髅标记）、拒绝进食与游戏、呼叫提醒。治疗需 1–4 剂随机药量（药量隐藏，连续喂直到康复）。

**死亡**：
- 生病后 24 宠物小时未治愈 → 死亡；
- 老年期每天 15% 概率自然死亡；
- 死亡画面：墓碑/天使 + 安魂音效 → 显示享年统计 → 长按中键 3 秒孵化新蛋（档案清零重来）。

### 3.7 小游戏：左右猜（复刻初代唯一游戏）

1. 宠物显示"背对/思考"画面；
2. 玩家用左/右键猜宠物将转向哪边，中键确认；
3. 5 回合，中 ≥3 为胜；
4. 每回合有按键音、结果音；胜利后宠物开心跳跃动画。

---

## 4. UI 规格

### 4.1 屏幕布局（240×240）

```
┌────────────────────────────────┐
│ 🔋88%   🕒 08:32        [🔔]   │ ← 顶栏 28px：电池、宠物时钟、注意指示
├────────────────────────────────┤
│  [🍚]   [💡]   [🎮]   [💊]     │ ← 上图标行 44px（4 个 56px 格）
│                                │
│                                │
│          宠 物 区               │ ← 124px：宠物动画 + 便便 + Zzz
│                                │
├────────────────────────────────┤
│  [🚽]   [📊]   [😤]    ·       │ ← 下图标行 44px（3 图标 + 空位）
└────────────────────────────────┘
```

- 选中图标：反色 + 闪烁光标（初代风格）。
- 整体视觉：经典拓麻歌子"液晶屏"质感 —— 素描风格像素画、低饱和配色、图标栏用分隔线。

### 4.2 像素资源清单（32×32 sprite，代码内 C 数组）

| 资源 | 数量 | 说明 |
|---|---|---|
| 蛋 | 2 帧 | 静止/摇晃 |
| 婴儿/儿童/少年 | 各 2 帧 idle | 呼吸摆动 |
| 成年 3 形态 + 老年 | 各 2 帧 idle | 形态差异明显 |
| 通用动作帧 | 吃饭×2、睡觉×1、生病×1、开心×2、委屈×1、死亡×1 | 全阶段复用 |
| 便便 | 1 | 可镜像摆位 |
| Zzz / 骷髅 / 心形（满/空） | 各 1 | 界面元素 |
| 8 个功能图标 | 8 | 选中/未选中两态（反色实现） |

> 实现方式：提供 Python 生成脚本（`tools/sprite_gen.py`），用 ASCII 画稿转 C 数组，用户可自行改画。

### 4.3 按键交互

| 按键 | 短按 | 长按（≥1.5s） |
|---|---|---|
| 左 / 右 | 图标焦点循环移动；子菜单/状态页翻页 | — |
| 中 | 进入功能 / 确认 / 返回 | 主界面：设置菜单（时间模式、音效开关、重置）；死亡画面：孵化新蛋（3s） |

按键驱动：GPIO 下降沿中断 + 20ms 软件去抖 + 事件队列。

---

## 5. 声音规格（ES8311 + I2S）

| 音效 | 波形 | 触发 |
|---|---|---|
| 按键 beep | 2kHz 方波 30ms | 任意按键 |
| 呼叫 | 三连 1.5kHz 脉冲 | 需求提醒（每 60s 重复一次直至响应） |
| 进食 | 上行双音 | 喂食成功 |
| 拒绝 | 低频短促双音 | 拒绝进食/误管教 |
| 治愈 | 上行琶音 | 每剂药 |
| 冲水 | 白噪声 400ms | 清洁 |
| 胜利/失败 | 琶音 / 下行音 | 小游戏结束 |
| 进化 | 闪亮音阶 | 阶段变化 |
| 孵化 | 欢快旋律 | 破壳 |
| 死亡 | 缓慢安魂旋律 | 死亡 |

实现：`audio` 组件内部合成 16kHz/16bit PCM 方波与包络，I2S DMA 播放；音量可关（设置菜单）。喇叭使能脚 GPIO21 控制省电。

---

## 6. 数据持久化（NVS）

- **保存时机**：每 5 分钟周期保存 + 关键事件（喂食/进化/入睡等）即时保存。
- **存储内容**：完整 `PetState` 结构（约 100 字节）+ 宠物纪元秒 + 上次保存时间。
- **开机恢复**：读取存档，直接继续（关机期间宠物暂停，符合 3.1 的时间设计）。
- **损坏处理**：CRC 校验失败 → 视为新游戏，进入孵蛋。
- NVS 命名空间 `boxpet`，key `state`。

---

## 7. 电源管理

| 策略 | 行为 |
|---|---|
| 背光超时 | 90 秒无操作 → 关背光（游戏逻辑继续运行），任意按键唤醒 |
| 睡眠联动 | 宠物入睡且用户关灯 → 30 秒后自动关背光 |
| 降频 | 空闲时 CPU 降至 80MHz（`esp_pm`），LVGL 渲染时自动升频 |
| 电池显示 | 顶栏百分比图标（GPIO1 ADC 采样，出厂标定系数可配置常量） |
| 充电指示 | GPIO48 读取充电状态，顶栏闪电图标 |

---

## 8. 软件架构

```
boxpet/
├── main/
│   ├── main.cpp              // 装配：初始化各模块，启动任务
│   ├── bsp/                  // 板级支持（与游戏逻辑解耦）
│   │   ├── board.cpp/.h      // LCD(ST7789)+背光+LVGL 装配
│   │   ├── buttons.cpp/.h    // 3 键驱动（中断→队列→LVGL 输入+游戏事件）
│   │   ├── audio.cpp/.h      // ES8311 初始化 + I2S 音效合成器
│   │   └── power.cpp/.h      // 电池 ADC、充电检测、背光超时
│   ├── game/                 // 纯逻辑（不依赖 LVGL，便于单测）
│   │   ├── pet.cpp/.h        // 状态机：衰减、生命周期、行为事件
│   │   ├── pet_def.h         // 全部数值表（本 spec 的第 3 章落成常量）
│   │   └── storage.cpp/.h    // NVS 存取 + CRC
│   ├── ui/
│   │   ├── ui_manager.cpp/.h // 场景切换：主界面/子菜单/小游戏/状态页/死亡
│   │   ├── scene_main.cpp    // 主界面（图标栏+宠物区渲染）
│   │   ├── scene_menu.cpp    // 喂食/设置等子菜单
│   │   ├── scene_game.cpp    // 左右猜小游戏
│   │   ├── scene_status.cpp  // 状态分页
│   │   └── anim.cpp/.h       // sprite 帧调度（按 stage/state 选帧）
│   └── sprites/
│       ├── pet_sprites.cpp   // 生成脚本输出的 C 数组
│       └── ui_sprites.cpp
├── tools/
│   └── sprite_gen.py         // 像素画 ASCII → C 数组（用户可改画）
├── CMakeLists.txt
├── sdkconfig.defaults
└── partitions.csv（如默认分区不敷使用再启用）
```

**任务模型（FreeRTOS）**：
- `gui_task`：LVGL 定时器 33ms（≈30fps），渲染当前场景；
- 游戏 tick：`esp_timer` 每 1 秒回调 `pet_tick()`，产出事件（衰减/进化/呼叫/生病…）推入事件队列；
- `ui` 消费事件刷新画面；`audio` 消费音效事件；
- 按键中断 → `button_queue` → 分发给 UI 与游戏。

**事件流**：`pet_tick() → GameEvent{FEED_OK, HUNGRY_CALL, EVOLVE, SICK, DIED…} → ui_manager / audio`。

---

## 9. 安装与烧录指南（交付物之一，随代码提供完整文档）

### 9.1 环境搭建（Windows）
1. 安装 VSCode + **ESP-IDF 扩展**，选择 ESP-IDF v5.4 在线安装（含工具链、Git、Python 依赖）。
2. 安装完成后验证：ESP-IDF 终端内 `idf.py --version` 正常输出。

### 9.2 编译与烧录
```bash
cd boxpet
idf.py set-target esp32s3
idf.py build
idf.py -p COM<x> flash monitor     # <x> 为设备管理器中的串口号
```
- BOX0 使用 Type-C 直连 ESP32-S3 原生 USB（内置 USB-Serial/JTAG），免驱；若未自动进入下载模式，按住「右键（GPIO0/BOOT）」再按复位松开。
- 出厂小智固件会被覆盖；如需恢复，可烧录 xiaozhi-esp32 的 `alientek-atk-dnesp32s3-box0` 固件（开源，同框架）。

### 9.3 首次运行
1. 上电 → 显示破壳动画 → 孵化婴儿形态；
2. 按「左/右」移动图标光标，「中」确认操作；
3. 中键长按进入设置：切换演示/真实模式、开关音效、重置存档。

### 9.4 常见问题排查（随代码文档提供）
- 串口找不到：换 Type-C 数据线（须支持数据传输）；
- 烧录失败：手动进下载模式（按住右键 + 复位）；
- 屏幕不亮：确认固件为本工程 BOX0 版本（背光 GPIO42）。

---

## 10. 非功能需求

| 项 | 指标 |
|---|---|
| 帧率 | 动画 ≥ 15fps，UI 操作响应 < 100ms |
| 内存 | LVGL 帧缓冲在 PSRAM；内部 SRAM 峰值占用 < 60% |
| 固件体积 | < 2MB（16MB Flash 默认分区充裕） |
| 掉电安全 | 任意时刻断电，重启后存档有效（NVS 原子写） |
| 续航 | 背光常亮 ≥ 2h；背光关闭模式下目标 ≥ 6h |

## 11. 里程碑

1. **M1 点亮**：屏幕 + 按键 + 主界面静态布局
2. **M2 核心循环**：宠物逻辑 + 动画 + 8 大功能
3. **M3 完整体验**：小游戏 + 音效 + 存档 + 电源管理
4. **M4 交付**：安装文档 + 真机验收
